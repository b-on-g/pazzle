namespace $.$$ {
	$mol_style_define($bog_pazzle_board_tile, {
		minHeight: '4rem',
		background: {
			repeat: 'no-repeat',
			color: $mol_theme.back,
		},
		boxShadow: `inset 0 0 0 1px ${$mol_theme.line}`,
	})
}
